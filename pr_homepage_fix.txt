## 📋 Resumo

Este PR aplica progressive disclosure na página inicial da wiki, corrigindo o problema de "linguiça de texto" que ainda persistia na homepage.

## 🎯 Problema Identificado

### Situação Atual
- A página inicial (`app/page.tsx`) estava renderizando o conteúdo do `ONBOARDING_PUBLICO.md` diretamente com `dangerouslySetInnerHTML`
- Não havia divisão em seções interativas (accordions)
- Conteúdo aparecia como "linguiça de texto" contínuo sem elementos clicáveis
- Progressive disclosure estava funcionando apenas em páginas de documentos individuais (`app/docs/[slug]/page.tsx`)

### Causa Raiz
O componente `ContentSections` que implementa progressive disclosure estava sendo usado apenas em páginas de documentos individuais, mas não na página inicial que também exibe conteúdo markdown extenso.

## ✅ Solução Implementada

### Mudanças em `app/page.tsx`:

1. **Importação do ContentSections:**
   - Adicionado import do componente `ContentSections` de `./docs/[slug]/content-sections`

2. **Geração de IDs para Headings:**
   - Adicionada lógica para gerar IDs automáticos em headings (h2, h3, h4)
   - IDs são gerados a partir do texto do heading (normalizado, sem acentos, com hífens)
   - Permite navegação e progressive disclosure funcionarem corretamente

3. **Substituição de Renderização Direta:**
   - Substituído `dangerouslySetInnerHTML` por `<ContentSections htmlContent={...} />`
   - Agora seções longas (>500 caracteres) são automaticamente colapsáveis com Accordion
   - Primeiras 2 seções abertas por padrão

## 📦 Arquivos Alterados

- `frontend/wiki/app/page.tsx` - Aplica progressive disclosure e geração de IDs

## ✅ Resultado Esperado

- ✅ Página inicial agora tem elementos clicáveis (accordions)
- ✅ Seções longas podem ser expandidas/colapsadas
- ✅ Elimina "linguiça de texto" na homepage
- ✅ Experiência consistente entre página inicial e páginas de documentos
- ✅ Navegação melhorada com IDs nos headings

## 🎯 Impacto

### Experiência do Usuário
- Navegação interativa na página inicial
- Conteúdo mais organizado e fácil de explorar
- Hierarquia visual clara com progressive disclosure
- Menos sobrecarga visual em conteúdo extenso
- Consistência entre todas as páginas da wiki
