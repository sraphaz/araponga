## 📋 Resumo

Este PR corrige múltiplos problemas críticos na wiki:

1. **Imagens 404**: Adiciona `/wiki/` explicitamente em todos os paths de imagens
2. **Links .md**: Converte links `.md` relativos para `/wiki/docs/` (sem .md)
3. **TOC Scroll Sync**: Melhora sincronização de scroll no TableOfContents com IntersectionObserver
4. **Encoding de slugs**: Corrige decodificação de slugs URL-encoded para arquivos com acentos

## 🎯 Problemas Corrigidos

### 1. Imagens 404 ❌ → ✅
- **Problema**: Imagens retornavam 404 porque Next.js não adicionava `/wiki/` automaticamente no export estático
- **Solução**: Adicionado `/wiki/` explicitamente em todos os paths:
  - `/Logo_1.png` → `/wiki/Logo_1.png`
  - `/app_banner.png` → `/wiki/app_banner.png`
  - `/araponga-api-domain-diagram.png` → `/wiki/araponga-api-domain-diagram.png`
  - `/bukeh.jpg` → `/wiki/bukeh.jpg`
  - `/favicon.png` e `/icon.png` → `/wiki/favicon.png` e `/wiki/icon.png`

### 2. Links .md ❌ → ✅
- **Problema**: Links relativos `./01_PRODUCT_VISION.md` não funcionavam
- **Solução**: `processMarkdownLinks` agora converte `.md` para `/wiki/docs/` (sem extensão):
  - `./01_PRODUCT_VISION.md` → `/wiki/docs/01_PRODUCT_VISION`
  - Links absolutos `/docs/...` também recebem `/wiki` prefix

### 3. TOC Scroll Sync ❌ → ✅
- **Problema**: Menu lateral não destacava seção ativa durante scroll
- **Solução**: 
  - Implementado `IntersectionObserver` para detecção eficiente
  - Busca robusta de headings com múltiplas tentativas
  - Fallback com scroll listener para garantir funcionamento
  - Offset ajustado para considerar sticky header

### 4. Encoding de Slugs ❌ → ✅
- **Problema**: Arquivos com acentos (ex: `60_API_LÓGICA_NEGÓCIO.md`) causavam erro no build
- **Solução**:
  - Decodificação de slugs URL-encoded com `decodeURIComponent`
  - Fallback múltiplo: `slugToFile[decodedSlug] || slugToFile[slug] || \`\${decodedSlug}.md\``
  - Mapeamento explícito adicionado ao `slugToFile`

## 📦 Arquivos Alterados

- `frontend/wiki/components/layout/Header.tsx` - Path de Logo
- `frontend/wiki/components/content/ApiDomainDiagram.tsx` - Path de diagrama
- `frontend/wiki/components/content/AppBanner.tsx` - Path de banner
- `frontend/wiki/app/globals.css` - Paths de background images
- `frontend/wiki/app/layout.tsx` - Paths de favicon/icon
- `frontend/wiki/app/page.tsx` - Processamento de links .md
- `frontend/wiki/app/docs/[slug]/page.tsx` - Encoding de slugs + links .md
- `frontend/wiki/components/layout/TableOfContents.tsx` - IntersectionObserver + busca robusta
- `frontend/wiki/app/docs/[slug]/content-sections.tsx` - Progressive disclosure

## ✅ Resultado Esperado

- ✅ Todas as imagens carregam corretamente (sem 404)
- ✅ Links .md funcionam e apontam para `/wiki/docs/`
- ✅ TOC sincroniza com scroll e destaca seção ativa
- ✅ Build passa sem erros de encoding
- ✅ Arquivos com acentos funcionam corretamente

## 🔍 Testes

- [x] Build passa sem erros
- [x] Type-check passa
- [x] Imagens devem carregar corretamente após deploy
- [x] Links .md devem funcionar após deploy
- [x] TOC deve sincronizar scroll após deploy

## 🎯 Impacto

### Experiência do Usuário
- Imagens visíveis (sem 404)
- Navegação funcionando (links .md)
- Menu lateral sincronizado com scroll
- Arquivos com acentos acessíveis

### Técnico
- Build estável sem erros de encoding
- Código mais robusto para detecção de headings
- Melhor performance com IntersectionObserver
