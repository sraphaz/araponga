/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // Adicione domínios aqui se vocês quiserem referenciar imagens remotas.
    ]
  }
};

export default nextConfig;
